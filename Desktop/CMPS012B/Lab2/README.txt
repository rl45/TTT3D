//-----------------------------------------------------------------------------
// README

//-----------------------------------------------------------------------------

FileReverse takes in a file, reads the informaiton side, and reverse outputs it into another file
FIleTokens basically splits a Stirng into an array and prints the lineNumber + the amount of FIleTokens
FileCopy reads in a file and pastes it into a different file